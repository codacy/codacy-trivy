// Package tool - xeol is used by the EOL scanner via the Go library in eol_xeol_lib.go (not the CLI).
// Build is fixed for xeol's transitive deps (syft, fangs) via go.mod replace directives.
package tool
